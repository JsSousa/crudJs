// Variables Globales
const formularioUI = document.getElementById('formProduct');
const productsListUI = document.getElementById('productsList');

let arrayProducts = [];

// Funciones
const CrearProduct = (formulario) => {

    console.log(formulario);

    let product = {
        name: '',
        quantity: '',
        price: '',
        boughtAt: ''
    }
}

// EventListener

formularioUI.addEventListener()